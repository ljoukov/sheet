# Gallery

SvelteKit gallery app for local development, visual QA, and screenshot capture.

Useful commands from the repo root:

```sh
npm run gallery:dev
npm run gallery:check
npm run gallery:build
```
